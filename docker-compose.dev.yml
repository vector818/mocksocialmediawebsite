# Development overrides — use with: docker compose -f docker-compose.yml -f docker-compose.dev.yml
# Note: SSL volume (/etc/letsencrypt) and port 443 from base are harmless in dev
# (nginx.dev.conf doesn't listen on 443, empty bind mount is created)
services:
  backend:
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2g
  frontend:
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2g
