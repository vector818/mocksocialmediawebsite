# Example environment configuration for external MySQL usage
# Copy to .env (or provide via --env-file) before running docker-compose.*external-db.yml

EXTERNAL_DB_HOST=your-rds-endpoint.amazonaws.com
EXTERNAL_DB_PORT=3306
EXTERNAL_DB_NAME=mocksocialmedia
EXTERNAL_DB_USERNAME=mocksocial
EXTERNAL_DB_PASSWORD=change-me
